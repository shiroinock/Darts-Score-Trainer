# git fetch, checkout main, pull

このコマンドは、リモートリポジトリから最新の変更を取得し、ローカルのmainブランチに切り替えて最新の状態に更新します。

## 手順
1. **リモートリポジトリから最新の変更を取得**：
   ```bash
   git fetch origin
   ```
2. **mainブランチに切り替え**：
   ```bash
   git checkout main
   ```
3. **mainブランチを最新の状態に更新**：
   ```bash
   git pull origin main
   ```
